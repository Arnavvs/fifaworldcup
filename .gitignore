# --- Large / regenerable data (NOT pushed; rebuild via the pipeline) ---
# Raw downloads: 6.6 GB incl. a 5.6 GB FIFA CSV — far over GitHub's 100 MB limit
fifa_wc_data/raw/
# SQLite DB is 122 MB (>100 MB limit) and regenerable from src/
fifa_wc_data/db/
*.db
*.db-journal
*.sqlite

# Compressed data dumps
*.csv.gz
*.zip

# --- Bulk data payloads (repo keeps docs + structure only) ---
# Processed data tables (regenerable); small dimension/structure CSVs are kept
fifa_wc_data/processed/
# Large model-ready datasets (regenerable via src/r6_exports.py)
research_ready_dataset/ml_match_features.csv
research_ready_dataset/classification_dataset.csv
research_ready_dataset/regression_dataset.csv
research_ready_dataset/tournament_dataset.csv

# --- Secrets (never commit) ---
.kaggle/
access_token
*.env
.env

# --- Python ---
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
venv/

# --- OS ---
.DS_Store
Thumbs.db

# v2 bulk datasets (regenerable via src/f1_build_features.py)
research_ready_dataset/match_features_v2.csv
research_ready_dataset/classification_dataset_v2.csv
research_ready_dataset/regression_dataset_v2.csv
